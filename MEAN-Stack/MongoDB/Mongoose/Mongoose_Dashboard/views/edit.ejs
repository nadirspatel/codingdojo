<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Mongoose Dashboard: Edit Animal</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <style>
        *{
            font-family: Arial,helvetica,sans-serif;
            font-size: 14px;
        }
        h1 {
            font-size: 20px;
            font-weight: bold;
        }
        .quote {
            padding-bottom: 10px;
        }
        .quotesList {
        width: 400px;
        border: 1px dashed blue;
        border-radius: 30px;
        padding: 20px;
        padding-bottom: 20px;
        }
        .messages {
        width: 400px;
        border: 1px dashed red;
        border-radius: 5px;
        }
        </style>
    </head>
    <body>
    <div class="container">

            <!-- tip use JSON.stringify to easily see objects printed in full instead of [object Object] -->
            <!-- based on the structure of your messages, determine how to display them -->
            <% if(messages.editanimal) { %>
            <%- JSON.stringify(messages) %> 
            <% for (var x of messages.editanimal) { %>
            <div class="messages">
            Error: <h4><%= x %></h4>
            </div>
            <% } %>
            <% } %>

        <div class="quotesList">
        <h1>Edit Animal</h1>
        <hr>
        <% for (let i=0; i<animals.length; i++) { %>
	    <form method="POST" action="/edit">
            <div class="quote">
            <input type="hidden" name="_id" value="<%= animals[i]._id %>">
            Animal Name: <input type="text" name="name" value="<%= animals[i].name %>"><br>
            Description: <input type="text" name="description" value="<%= animals[i].description %>"><br>
            <input type="submit" value="Update">
        </form>
        <% } %>
            </div>
        </div>


    </div>
</body>
</html>
