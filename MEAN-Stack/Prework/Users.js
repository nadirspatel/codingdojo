function userLanguages(users) { // object constructor

  var users = new 

    this.fname = users.fname; // first name object
    this.lname = users.lname; // last name object
    this.languages = users.languages; // languages object
    this.interests = users.interests; // interests object
    console.log(this.fname)
}